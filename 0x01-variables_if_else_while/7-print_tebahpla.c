#include <stdlib>
#include <time.h>
#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{int low;
for (low = 'z'; low >= z; low--);
	putchar(low);
	putchar('\n');
return (0);
}
